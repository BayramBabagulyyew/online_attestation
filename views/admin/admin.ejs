<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=pageTitle%></title>
    <link rel="stylesheet" href="css/admin.css" />
  </head>
  <header role="banner">
    <h1>Bilim admin</h1>
    <ul class="utilities">
      <br />
      <li class="users"><a href="/admin/profile?id=<%=id%>">Hasabym</a></li>
      <li class="logout warn">
        <a href="/admin/logout?id=<%=id%>">Çykmak</a>
      </li>
    </ul>
  </header>

  <nav role="navigation">
    <ul class="main">
      <li class="dashboard"><a href="/admin?id=<%=id%>">Umumy</a></li>

      <li class="write">
        <a href="/admin/add-test?id=<%=id%>" method="POST">Testler</a>
      </li>
      <li class="users"><a href="#">Ulanyjylar</a></li>
    </ul>
  </nav>

  <main role="main">
    <section class="panel important">
      <h2>Toparlar</h2>
      <ul>
        <% if (groupUser) { %> <% for (let group of groupUser) { %>

        <li>
          <h2>Topar: <b><%= group.group%></b></h2>
          <p>Diňleýjiler: <b><%=group.count%></b></p>
          <% let k=0 %> <% for (let user of group.users) { %>
          <b><%= ++k; %>. </b>
          <%=user.username%>
          <br />
          <% } %>
        </li>
        <hr />

        <% } } %>
      </ul>
    </section>
  </main>
</html>
